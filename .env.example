# Backend-only configuration. Never expose GITHUB_TOKEN to the web application.
NODE_ENV=development
PORT=3000
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:4b-instruct
GITHUB_TOKEN=

# Browser-safe configuration. Only PUBLIC_ variables may be bundled into the web app.
PUBLIC_API_BASE_URL=http://127.0.0.1:3000
