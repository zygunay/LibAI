{
  "name": "@libai/model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "benchmark:ollama": "pnpm run build && node scripts/benchmark-ollama.mjs",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  }
}
